# Copy to .env and adjust for your deployment.
# ALLOWED_ORIGINS must include the exact origin(s) your frontend is served from.
ALLOWED_ORIGINS=["http://localhost:5173","https://your-frontend-host.example.com"]
